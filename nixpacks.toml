# Railway Nixpacks deployment configuration
[phases.setup]
nixPkgs = ["nodejs_20", "openssh"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
